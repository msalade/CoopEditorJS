import React from 'react';

const Layout = ({ child }) => (
    <div>
        <header>

        </header>
        <body>
            {child}
        </body>
        <footer>
            
        </footer>
    </div>
)

export default Layout;